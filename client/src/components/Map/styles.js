import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
    map: {
        width: '1280',
        height: '80vh',
        position: 'relative',
        alignItems: 'center',
        marginBottom: '20px'
    }
});